<ul class="nav navbar-nav navbar-right">
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> <span class="glyphicon glyphicon-shopping-cart"></span>
      <%= @cart.items.size %> - Items
      <span class="caret"></span></a>
    <ul class="dropdown-menu dropdown-cart" role="menu">
<% @cart.items.each do |item| %>
          <li>
                  <span class="item">
                    <span class="item-left">
                        <img src="http://lorempixel.com/50/50/" />
                        <span class="item-info">
                            <span><%= link_to item.name, item_path(item) %></span>
                            <span><%=item.price %>$</span>
                        </span>
                    </span>
                    <span class="item-right">
                      <button class="btn btn-xs btn-danger pull-right"> <%= link_to 'x', cart_remove_path(item.positions.first.id),method: :delete %></button>
                    </span>
                </span>
          </li>
<% end %>
      <li class="divider"></li>
      <li><a class="text-center" href="">View Cart</a></li>
    </ul>
  </li>
</ul>
